# include("type.jl")

# function dist(p1::Point, p2::Point)
#     sqrt((p1.x-p2.x)^2 + (p1.y-p2.y)^2 + (p1.z-p2.z)^2)
# end

# function internal_division_point(p1::Point, p2::Point)
#     v1 = abs(p1.val)
#     v2 = abs(p2.val)
#     Point((v2*p1.x + v1*p2.x) /(v1+v2), (v2*p1.y + v1*p2.y)/(v1+v2), (v2*p1.z + v1*p2.z)/(v1+v2), 0.0)
# end

